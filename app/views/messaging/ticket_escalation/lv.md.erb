# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: Eskalēts plkst #{ticket.escalation_at}_
Pieteikums (#{ticket.title}) no "#{ticket.customer.longname}" eskalēts  no "#{ticket.escalation_at}"!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
