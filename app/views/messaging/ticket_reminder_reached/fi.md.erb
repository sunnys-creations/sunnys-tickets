# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Tiketti##{ticket.number}>: Muistutus saavutettu!_
Tiketti vaatii huomiota: muistutus saavutettiin (#{ticket.title}) asiakkaalla "*#{ticket.customer.longname}*".

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
