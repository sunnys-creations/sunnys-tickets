# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: Izveidoja #{current_user.longname} at #{ticket.updated_at}_
* Grupa: #{ticket.group.name}
* Īpašnieks: #{ticket.owner.fullname}
* Statuss: #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
