Ще одна заявка була об'єднана в заявку (#{ticket.title})

<div>Привіт #{recipient.firstname},</div>
<br>
<div>
Ще одна заявка була об'єднана в заявку (#{ticket.title}) користувачем "<b>#{current_user.longname}</b>".
</div>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">Переглянути на #{config.product_name}</a>
</div>
