fragment overviewAttributes on Overview {
  id
  name
  link
  prio
  groupBy
  orderBy
  orderDirection
  viewColumns {
    key
    value
  }
  orderColumns {
    key
    value
  }
  organizationShared
  outOfOffice
  active
  viewColumnsRaw
  ticketCount @include(if: $withTicketCount)
}
